#include "Coordinates.h"

#include <math.h>
/*
Coordinates::Coordinates() {
  X = 0;
  Y = 0;
}

Coordinates::Coordinates(float _x, float _y) {
  X = _x;
  Y = _y;
}

float Coordinates::getX() { return X; }
float Coordinates::getY() { return Y; }
void Coordinates::setX(float _x) { X = _x; }
void Coordinates::setY(float _y) { Y = _y; }

bool Coordinates::operator==(const Coordinates &obj) {
  float diffX = fabs(X - obj.X);
  float diffY = fabs(Y - obj.Y);

  return (diffX <= 0.0001f && diffY <= 0.0001f);
}
*/
