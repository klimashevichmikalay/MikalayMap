#include "FilledPolygon.h"
/*
using namespace figureTypes;

Coordinates FilledPolygon::getStart() {
  if (points.size() > 0)
    return points.at(0);
  else
    throw std::logic_error("points in filledPolygon is empty");
}

Coordinates FilledPolygon::getEnd() {
  size_t sz = points.size();

  if (sz > 0)
    return points.at(sz - 1);
  else
    throw std::logic_error("points in filledPolygon is empty");
}

FilledPolygon::FilledPolygon() : LineString() { type = FILLED_POLYGON; }

FilledPolygon::FilledPolygon(const std::string &_name) : LineString(_name) {
  type = FILLED_POLYGON;
}

FilledPolygon::FilledPolygon(const char *_name) : LineString(_name) {
  type = FILLED_POLYGON;
}
*/
