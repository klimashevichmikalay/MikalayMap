/*#include "Scale.h"

using namespace std;

void Scale::multCoordinates(std::vector<Coordinates> &points, float _factor) {
  for (auto it = points.begin(); it != points.end(); ++it) {
    it->setX(it->getX() * _factor);
    it->setY(it->getY() * _factor);
  }
}

void Scale::minusCoordinates(std::vector<Coordinates> &points,
                             Coordinates _delta) {
  for (auto it = points.begin(); it != points.end(); ++it) {
    it->setX(it->getX() - _delta.getX());
    it->setY(it->getY() - _delta.getY());
  }
}
*/
